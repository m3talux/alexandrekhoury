# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: touch .env.local
      - run: echo 'NEXT_PUBLIC_GA_MEASUREMENT_ID="${{ secrets.GA_MEASUREMENT_ID }}"' > .env.local
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_ALEXANDREKHOURY_3A0AA }}'
          channelId: live
          projectId: alexandrekhoury-3a0aa
          firebaseToolsVersion: 12.4.4
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
